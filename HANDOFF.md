# Sugoroku2（すごろく人生ゲーム）HANDOFF

**2026-08-12: SugorokuApp リポジトリから独立した単独リポジトリになった。**
`github.com/Sekiguchi-Takashi/Sugoroku2`（private / main）。旧 `SugorokuApp/human/` は廃止。
A面（どうぶつすごろく）と `bside/` は旧リポジトリに残っており、こちらからは一切参照しない。

| 項目 | 値 |
|---|---|
| モジュール | `app/`（単一モジュール） |
| applicationId | `com.appathy.sugoroku.human`（旧リポジトリ時代と同じ。**署名も同じ debug.keystore を引き継いだので上書き更新できる**） |
| ラベル | すごろく人生ゲーム |
| CI | `.github/workflows/build.yml` → artifact `Sugoroku2-debug-apk` / `app-debug.apk`（Maven Central 403 対策のリトライ入り） |
| 納品 | `Sugoroku2_vX.X.zip`（トップフォルダ名は固定 `Sugoroku2`）＋ 直下の `deploy.sh` |
| 検証 | `python3 tools/validate.py` |

## 取り込み手順
```
cd ~
cp /sdcard/Download/Sugoroku2_vX.X.zip .
unzip -o Sugoroku2_vX.X.zip
bash ~/Sugoroku2/deploy.sh "vX.X 要約"
```

## ビルド規約（変更禁止）
- AGP 8.5.2 / Kotlin 1.9.24 / Gradle 8.9（CIの setup-gradle で固定、wrapperなし）
- 外部依存ゼロ・XMLレイアウト不使用（AndroidManifest.xml を除きすべてプログラマティックUI）
- `debug.keystore` はリポジトリ直下にコミット済み（storepass/keypass: `android`、alias: `androiddebugkey`）
- Kotlin文字列テンプレート罠：`$変数` の直後に日本語でビルド失敗。本ソースは `+` 連結のみ

## ゲーム仕様（v3.8 = 205マス盤 + A面UI + ぶかつ + こい）
人生すごろく。あかちゃん(0-19 / 20マス) → ほいくえん(20-44 / 25) → しょうがっこう(45-84 / 40) → ちゅうがっこう(85-124 / 40) → こうこう(125-164 / 40) → だいがく・しんしゃかいじん(165-204 / 40) の**205マス**。
盤面データは `app/src/main/assets/events_human.json` のみ。マス数・ステージ範囲を変えるときは `stages` の from/to と `cells` の `i` を必ず連番で揃える（`tools/validate.py` が検査する）。

**205マスのJSONは手書きしない。正本は `tools/gen/` の生成スクリプト。**
```
python3 tools/gen/build.py     # board_a.py + board_b.py → events_human.json（bgの90マス絞り込みも自動）
python3 tools/gen/sim.py       # 400回×4人のシミュレーションで need と到達率を実測
python3 tools/validate.py      # 納品前チェック（res/drawable が無ければ tools/asset_list.txt を使う）
```
- `tools/gen/board_a.py` … baby / kinder / elem、`tools/gen/board_b.py` … jhs / high / univ
- `build.py` は「表示されないマスに書かれた背景を近くの表示マスへ寄せる」「背景の種類のカバー率を優先して90マスまで絞る」処理を持つ。だから board_*.py には気にせず `bg=` を書いてよい
- 実行すると マス数／通過マス率／背景の使用・未使用／CHALLENGEの need と基準値 がレポートされる

- ステータス4種：べんきょう / うんどう / にんき / おこづかい（初期 5 / 5 / 5 / 1000）
- キャラ2セット：`sets.human`＝プレイヤー6人（あかり・みなみ・さくら・はやと・けんと・たける）、
  `sets.partner`＝恋人候補6人（ひまり・しおり・ゆい・そうた・りく・あおい）
- マス種別：START / NORMAL / GOOD / BAD / WARP / REST / CHOICE / RANDOM / AGAIN / CRUSH / CHALLENGE / **CLUBEVENT** / **DATE** / **LOVE** / **STAGEGOAL** / GOAL
- **ぶかつ（v3.7〜）**：`clubStages`（`jhs` / `high` / `univ`）に はいった しゅんかん、STAGEGOALのワープ直後に**全員が1人ずつ必ず1つ選ぶ**（マスではなくステージ入場イベントなので、踏み逃しが起きない）。10種：やきゅう／サッカー／テニス／バスケット／バレーボール／ゴルフ／しょうぎ／かがく／かていか／きたく
  - 定義は `events_human.json` の `clubs`。`d`＝ぶかつマス1回ぶんの のび、`join`＝入部時の のび（`d`のおよそ半分、`tools/gen/build.py` が自動計算）
  - 🏅**CLUBEVENTマス**（9マス）の効果と文章と背景は「入っている部活」で決まる。だからセルには `bg` を書かない（`build.py` が CLUBEVENT を bg 割り当て対象から外している）
  - `"deai": true` の CLUBEVENT（101 ごうしゅく / 128 ぶかつの せんぱい / 166 サークルの あさ）では、まだ相手がいなければ部活仲間との**出会い**が起きて `crush` が決まる
  - ステージごとに選び直せる。人間は「つづける」が先頭に出る／CPUは60%で同じ部活を続ける
  - 部活ごとの専用画像はまだ無く、既存の背景を流用している（かがく=`bg_science_room` / かていか=`bg_cooking_room` は本来の絵。やきゅう=校庭、サッカー=夕暮れのグラウンド、テニス=高校校庭、バスケ・バレー=体育館、ゴルフ=草原の道、しょうぎ=図書室、きたく=モール）
- **タイプ（v3.8〜）**：ぶかつを選んだ直後、同じくステージ入場時に全員が📘べんきょう／🏃うんどう／💗こい／⚖バランス から1つ選ぶ。**選ばれた数だけそのタグのマスが盤面に並ぶ**（`pools` の候補20枚から、そのステージの10スロットを重み付き抽選で貼り替える）。重みは `study/sport/love` が各 `1 + 3×人数`、無属性 `free` が 2、バランスは3タグに+1ずつ
  - 盤面は共有なので**個人ではなく全員の投票で決まる**。1人だけ「こい」を選んでもデートマスは増えるが、全員で選ぶと最大まで増える
  - スロット位置は `tools/gen/build.py` が自動選定（START/GOAL/STAGEGOAL/CHALLENGE/CLUBEVENT/WARP/REST/AGAIN/DATE/LOVE と通過マスは除外）
- **こい（v3.8〜）**：プレイヤーが使っていない異性6人に、プレイヤーごとの**こうかんど 0〜10**（♥表示）を持つ
  - ☕**DATEマス**：ランダムな3人だけ出て、選んだ相手のこうかんど +3。こいびとがいる場合は2人でおでかけ（にんき+2）
  - 💞**LOVEマス**：こうかんど5以上の相手がいると、`25+こうかんど×3`%で**相手から告白**、そうでなければ「こくはくする／やめておく」の選択肢。成功率は `20 + こうかんど×7 + にんき/3`。成立で こいびと＋にんき+6＋もくひょう `love` 達成
  - こいびとがいるとLOVEマスは 70% で旅行・プレゼント・長話（にんき+3〜4、おこづかい減）、**30% でけんか**（にんき-2）。けんかが**3回連続で別れ**（にんき-4、相手のこうかんどは2に戻る）。けんか以外のこい系イベントで連続カウントはリセット
  - 🏅CLUBEVENTの `deai` マスは、こいびとがいなければランダムな1人のこうかんど +3
  - 旧 `CRUSH` マスと「こくはく」CHALLENGE は廃止（コードは互換のため残置）。DATE/LOVE は各ステージに最低1つずつ固定で置き、入れ替え対象にしない
  - 1ゲーム1人あたりの遭遇回数（実測）：全員こい＝デート2.4回／こい2.6回、こい1人＝1.8回／1.8回、こい0人＝1.2回／1.4回
- **ステージ先頭のマスには効果を置かない**：STAGEGOALで全員がワープしてくる先なので、誰も「止まる」判定を通らず `onLanded` が呼ばれない。v3.7 で各ステージ先頭を通過マス（もん・こうもん など）に統一し、にゅうがく系イベントと `CRUSH` は1〜2マス後ろへ移した
- **STAGEGOAL**：各ステージの最終マス。誰か1人が到達したら全員が次ステージの先頭へワープする。到達者は stageWins+1（+10点）。ルーレットで行き過ぎることはなく、ステージの最終マスで止まる
- **通過マス**：`NORMAL` で `st/sp/pp/mn/move/rest` を1つも書かないマスは、ダイアログを出さずログだけ流れて自動で次へ進む。全205マス中48マス（23%）がこれ
- 背景：セルに `"bg": "bg_library"` を書くと、そのイベントのダイアログ上部に画像が出る（`res/drawable/bg_*.jpg`、横長800px・JPEG品質80）。現在90マスに割り当て（86枚中78種を使用）
  - **背景が出るのは `message()` を開くマスだけ**。CHOICE / CRUSH は `AlertDialog` の素の本文、通過マスはダイアログ自体が無いので、そこに `"bg"` を書いても表示されない
- もくひょう3種：`exam`（108 こうこうじゅけん / 146 さいごのしけん / 180 しかくしけん）・`sports`（90 たいかい）・`love`（126 こくはく）。CHALLENGEに `"goal"` を付けると成功時に達成。1個+20点
- **CHALLENGEの `need` は「全マスを踏んだ合計」ではなく実測で決める**：ルーレットは1〜6なので実際に止まるのは全体の約3割。205マス盤の実測中央値は べんきょう51 / うんどう29 / にんき55 で、`need` はその時点の中央値に合わせてある（成功率0.47〜0.58）。**マスを増やしたら need も上げ直すこと**（v3.7 時点で 16 / 18 / 24 / 33 / 30 / 45 / 51、成功率0.51〜0.61）
- **ステージ末尾のマスは踏まれにくい**：先頭の1人が STAGEGOAL に着くと全員ワープするため、末尾3〜4マスの到達率が落ちる。受験など重要なCHALLENGEはステージ終わりの5〜7マス手前に置く
- **第1章は だいがく・しんしゃかいじん でゴール。しゃかいじん／ろうご は第2章として別に作る**
- 恋人：DATEでこうかんどを上げ、LOVEマスで告白して成立（v3.8で仕様変更）。得点+15
- 2〜4人プレイ（人の数を選び残りはCPU）、全員ゴールで順位＋エンディング4種
- **盤面はA面（SugorokuApp v5.5-A）と同じスタイル**：写真背景（ステージごとに bg_*.jpg を流用、カメラ0.25倍のパララックス・左右反転タイリング）＋フラットな円マス（種別ごとの色＋記号）＋盤面上部に「いまのステージ全体」のミニマップ＋手番のコマは大きく はねる
  - ステージ背景の対応：baby=bg_park_day / kinder=bg_playground / elem=bg_schoolyard / jhs=bg_school_route / high=bg_highschool_day / univ=bg_town_crossing（BoardView.boardBgName）
  - マスの記号：S / G / 🚩STAGEGOAL / 🏅CLUBEVENT / ☕DATE / 💞LOVE / 🌸CHALLENGE / 💗CRUSH / ⭐GOOD / 💧BAD / 🌀WARP / 💤REST / ❓CHOICE / 🎲RANDOM / 🔁AGAIN、通過マスは番号
  - ズームボタン🔍で 3 / 5 / 8マス表示を切替（A面と同じ Zoom オブジェクト）。205マスでも全体ミニマップにせず**ステージ単位**で見せる（40マスなら点が読める）
  - こくはく成立後は恋人のコマが隣に並ぶ（A面の結婚パートナー描画と同じ寸法）
- 画面遷移：`showTitle()` → `showModeSelect()`（人数とCPU数を1画面で選択）→ `showCharaSelect()` → `playIntro()` → `startGame()` → `showResult()`
- タイトルとキャラ選択のサムネイルは `previewStageKey()`＝`high` 固定。盤面の駒だけが現在ステージの姿になる

## アイコン・アプリ名
- アプリ名は `app/src/main/AndroidManifest.xml` の `android:label`（現在「すごろく人生ゲーム」）とタイトル画面の文言の2箇所
- アイコン `app/src/main/res/drawable/ic_launcher.png`（432px）は街角背景 3377.png の (560,330)-(1020,768) を切り出したもの。
  他の候補：桜の木の寄り(40,230)-(500,690) / コンビニ角(900,60)-(1408,568) / 信号寄り(1000,150)-(1360,510)
- ※「人生ゲーム」はタカラトミーの登録商標。配布範囲が身内・テスターを超える場合は名称の再検討が必要

## 素材の規約
**生成プロンプトの正本は `app/ASSET_PROMPTS.md`**（人物の固定要素表・ステージ別の服装指定・向き違いの指定）。
現在の収録状況：
- プレイヤー6人：`baby` `kinder` `elem` `jhs` `high` `univ` すべて済（盤面の全ステージをカバー）
- 恋人候補6人：`elem`（旧 `chara_kid` 画像＝暫定）／`jhs` `high` `univ` 済。`baby` `kinder` は未収録だが、恋人の登場は115マス目以降なので実害なし
- 未収録ステージ：`work` `senior`（盤面は しゃかいじんに なる でゴール）
- 立ち絵：`chara_<stageKey><NN>.png`。stageKey = `baby / kinder / elem / jhs / high / univ / work / senior`
  - 向き違いは `_s`（側面）/ `_b`（背面）を同名に付けて置くと盤面の駒が自動で使う。無ければ正面へフォールバック
  - `charas_human.json` の `images` に1行足すだけで反映。未定義ステージは近いステージの絵で代用
- 紹介ムービー：`app/src/main/res/raw/intro_<基準画像名>.mp4`（例 `intro_chara_kid12.mp4` ＝さくら）。無いキャラはスキップ
  - プレイヤーの並び順に依存させないため、キャラの `img`（elem の基準画像名）に紐づける方式
  - H.264 baseline / yuv420p / AAC / 1280×720 / 10秒前後。`androidResources { noCompress += "mp4" }` 指定済み
  - マゼンタ背景は ffmpeg の `colorkey=0xE30BE3:0.32:0.10,despill` で差し替えてから収録

## 落とし穴
- Kotlin文字列テンプレート罠：`$変数` の直後に日本語でビルド失敗。本ソースは `+` 連結のみ
- 納品前に `python3 tools/validate.py`（`tools/gen/build.py` を回したあとは必ず）
- ルートの `build.gradle.kts` に AGP 8.5.2 / Kotlin 1.9.24 が `apply false` で宣言されている前提

## バージョン履歴
| Ver | 内容 |
|---|---|
| v3.8 | **こいシステムを作り直し、タイプ選択で盤面が変わるようにした**。ステージ入場時に「ぶかつ→タイプ」を全員が選び、選ばれたタイプのマスが10スロットに貼り替わる（`pools` / `types` を JSON に追加）。CRUSH と こくはくCHALLENGE を廃止して、こうかんど制の ☕DATE / 💞LOVE に置き換え。デートは3人ランダム提示、告白は相手からもある、成立後は旅行・けんか・3回連続で別れ。ステータス画面にこうかんどのバーとけんか回数を表示。`tools/validate.py` に pools/types/こいマスの検査、`tools/gen/sim.py` にこいの進行を追加 |
| v3.7 | **ぶかつギミックを追加**。ちゅうがっこう／こうこう／だいがく に入るたび、全員が10種類から1つ必ず選ぶ（ステージ入場時のダイアログ。CPUは自動、60%で続投）。部活ごとに伸びるステータスと文章と背景が変わり、🏅CLUBEVENTマス9個で効果が出る。`"deai"` のマスでは部活仲間との出会いで `crush` が決まる。**ステージ先頭マスが誰にも踏まれない問題を修正**（STAGEGOALのワープ先なので `onLanded` が通らず、にゅうがくの効果とCRUSHが不発だった）。need を再調整。`tools/gen/sim.py` と `tools/validate.py` も部活対応 |
| v3.6 | **乳幼児・ほいくえんのイベントを追加して 195→205マス**（あかちゃん15→20 / ようちえん20→25、ステージ名も **ほいくえん** へ）。背景11枚を追加（`bg_baby_room` `bg_baby_bath` `bg_baby_shop` `bg_nursery_room` `bg_zoo` `bg_aquarium` `bg_amusement_wide` `bg_carousel` `bg_circus` `bg_campsite` `bg_farm`、800px・JPEG品質80）＝合計86枚。新イベント：はじめてのおふろ／ベビーベッドのよる／ベビーようひんてん(RANDOM)／ゆびさし／はいはいきょうそう(RANDOM)／ほいくしつのじゆうあそび／すいぞくかん／ぼくじょうたいけん／サーカス／はじめてのキャンプ／メリーゴーラウンド／しゅうまつのおでかけ(CHOICE)。えんそくは どうぶつえん の絵に、ゆうえんちは全景の絵に差し替え。**CHALLENGEの need を再調整**（早いステージが増えてステータスが上振れしたため）。bgの上限を80→90に、`tools/validate.py` は res/drawable と asset_list.txt の**和集合**で検証（納品ZIPには新規画像だけが入るため） |
| v3.5 | **盤面まわりのUIをA面 v5.5-A に完全一致させた**。擬似3D盤面を廃止し、写真背景＋パララックス＋フラット円マス＋記号＋ステージ別ミニマップ＋手番コマのバウンドに変更。レイアウト順もA面と同じ「盤面(上・weight1) → 凡例+🔍ズーム/はやさ → 手番見出し → ログ → ルーレット+ボタン → ステータスバー」。ルーレットをパステル6色・白枠・TAP中央・上向きピン・**結果の数字ポップ**（snapSpinカーブ含む）に差し替え。下部ステータスバーは手番プレイヤー1行表示（全員ぶんはステータスボタン）。ズームは3/5/8マス切替 |
| v1.0 | 初版。45マス3ステージ、12キャラ、選択肢・チャレンジ・ワープ・休み、結果とエンディング4種 |
| v1.1 | 中学生画像12枚追加、ライフステージ別の駒画像切替、盤面の擬似3D化 |
| v1.2 | プレイヤー6人＋恋人候補6人に分離、CRUSHマスと恋人成立 |
| v1.3 | キャラ決定時の紹介ムービー（さくら） |
| v1.4 | `bside/` から `human/` へモジュール移設（A面チャットのB面と衝突したため）。機能変更なし |
| v1.5 | アプリ名を **人生ゲーム** に変更（`android:label` とタイトル画面）。アイコンを街角背景（横断歩道＋桜）の切り出しに差し替え（432px） |
| v1.6 | アプリ名を **すごろく人生ゲーム** に変更（タイトル文字は9文字なので30spへ縮小） |
| v1.7 | 高校生の立ち絵12枚を追加（`chara_high01..12`、背景透過・512px）。紹介ムービーの命名を基準画像名基準へ変更（`intro_chara_kid12.mp4`）。生成プロンプト仕様書 `app/ASSET_PROMPTS.md` を追加 |
| v1.8 | 赤ちゃんの立ち絵6枚（プレイヤーのみ `chara_baby01/02/07/09/10/12`）を追加。**盤面の先頭に「あかちゃん・ようちえん」8マスを追加して45→53マス**。以降のマス番号は+8シフト（CRUSH 38 / こくはく 42） |
| v1.9 | 幼稚園の立ち絵6枚（`chara_kinder01/02/07/09/10/12`）を追加。先頭ステージを **あかちゃん(0-3) / ようちえん(4-7)** の2つに分割し、4マス目から園服の姿になる |
| v2.0 | 小学生の立ち絵6枚（`chara_elem01/02/07/09/10/12`）を追加し、暫定だった私服画像を置き換え。**プレイヤー6人はあかちゃん〜こうこうの5ステージすべて収録完了** |
| v2.1 | 大学生の立ち絵6枚（`chara_univ01/02/07/09/10/12`）を追加。**盤面に「だいがく・しんしゃかいじん」10マスを追加して53→63マス**。旧ゴール(52)は「こうこう そつぎょう」に変更し、新ゴールは62「しゃかいじんに なる」 |
| v2.2 | 恋人候補6人の大学生の立ち絵（`chara_univ03/04/05/06/08/11`）を追加。**12人全員が `univ` まで揃った**。ただし しおり(06)・りく(05) は制服姿のため、大学生として違和感があれば差し替え候補 |
| v2.4 | CIワークフローに Maven Central 403（Sonatype側CDNの一時ブロック）対策のリトライを追加（アプリ本体の変更なし） |
| v3.4 | **盤面を63→195マスに拡張**（あかちゃん15／ようちえん20／しょうがっこう40／ちゅうがっこう40／こうこう40／だいがく40）。各ステージ末は STAGEGOAL、何も起きない通過マスを49マス（25%）、背景を80マスに割り当て（68種）。CHALLENGEの `need` を実測ベースで再設定（18/27/33/12/14/25/40）、おこづかいの収支を再調整。ステージごとの空・地面の色を6段階に。マス色に STAGEGOAL / CRUSH / AGAIN / RANDOM を追加。**WARPがステージをまたがないよう `clampStage()` を追加**（またぐと STAGEGOAL を踏まずに先へ行けてしまうバグ）。盤面描画は毎フレーム195マスぶんの道と並べ替えを作らず、見えている範囲だけに限定。盤面の生成スクリプトを `tools/gen/` としてリポジトリに収録し、`tools/validate.py` に「ステージ構成195マス・通過マス率・背景の割り当て先」の検査を追加（画像が無い納品ZIPでは `tools/asset_list.txt` を参照） |
| v3.3 | **SugorokuApp から独立して単独リポジトリ Sugoroku2 に移行**。`human/` → `app/`、ルートの build/settings/gradle.properties と debug.keystore（A面から引き継ぎ＝署名同一）、CIとdeploy.shを新設。機能変更なし |
| v3.2 | **UIをA面（SugorokuApp v5.5-A）に合わせた**。イベントは「背景画像(FIT_CENTER)＋自分と相手の立ち絵をボトム重ね＋白い角丸メッセージ枠(16sp/#263238/緑枠)＋OK」。操作は**スタート！ボタン**（オレンジ #FF9800・20sp）でルーレットを回す方式。ルーレットは高さ200dp・weight 1.3。上部に「ステージn/N「名前」＋凡例」(11sp #558B2F)と手番見出し(17sp太字 #33691E)、下部に濃緑(#33691E)のステータスバー。速度はA面と同じ `Speed` オブジェクト（ふつう／はやい⚡をボタンで切替：spin 2600/1100・step 300/140・result 700/300・cpu 1000/400・event 700/350） |
| v3.1 | **1マス=1タップに統一**。CHOICE・CRUSH の「選ぶ→結果ダイアログ→OK」の2タップを解消し、選択後の結果はトースト＋ログで自動送り（`flash()`）。テンポ定数を先頭にまとめて全体を高速化（1マス220→130ms、ルーレット2100→1300ms、CPU待ち1200→700ms、自動送り550ms） |
| v3.0 | 背景15枚を追加して**合計75枚**（美術室・音楽室・卒業式・式典ホール・塾の自習室・受験勉強コーナー・部室・運動会・夕暮れのグラウンド・廊下・木の下駄箱・夕暮れの教室・桜の通学路・古い街並み・都会の窓辺）。アイテム画像 `item_hat` `item_recorder` も追加。drawable 合計 15.5MB |
| v2.9 | 背景をさらに20枚追加して合計60枚（花見・桜の川辺・渡り廊下・下駄箱・保健室・屋上・図書室・理科室・家庭科室・職員室・教室（小/高）・通学路・街角の交差点・公園の芝生・モール明るめ・森・渓流・デスクワーク・草原の道）。まだセルには未割り当て |
| v2.8 | 背景を20枚追加して合計40枚（旅館・遊園地・客船ロビー・駅ホーム昼夜・観覧車ゴンドラ・屋内プール・スキー場・パリ・田舎駅（雨/朝焼け）・公園（晴/雨/夜）・高校校庭（晴/雨）・ボウリング場・森の小屋・モール・スキー斜面）。まだセルには未割り当て |
| v2.7 | 背景画像20枚（`bg_*.jpg` / 横長800px / 平均82KB）を取り込み。**STAGEGOAL** マス種別を追加し、各ステージの最終マスに配置。誰か1人が到達すると全員が次ステージの先頭へ移動（1位は+10点）。移動はステージ内でクランプ。イベントダイアログに `"bg"` の画像を表示 |
| v2.6 | B面ワークフローに paths フィルタ（`human/**` 等が変わったときだけ起動） |
| v2.5 | タイトル/キャラ選択で見せる姿を **高校生固定**（`previewStageKey()`）に変更。人数選択の2画面を **あそびかた選択1画面** に統合してタイトルへ戻るループを解消。キャラ選択の下端ボタンは「もどる」であそびかた選択へ。キャラ未読込時のガードを追加 |
| v2.3 | **もくひょう3種**（じゅけん せいこう / たいかい ゆうしょう / こいびとが できる）を導入。CHALLENGEセルの `"goal"` で達成判定し、成績欄に★数、結果画面に達成状況、得点+20/個。新マス種別 **AGAIN**（もう一度まわせる）と **RANDOM**（結果がランダムな2択）を追加 |

## 次にやる候補
1. 1ゲームの長さ調整（205マスだと4人プレイでルーレット約196回）。ルーレットの目を1〜6から広げる／ステージ数を選べるようにする、など
2. CHOICE と CRUSH のダイアログにも背景画像を出す（今は `message()` のマスだけ）。対応すれば背景の割り当て先を+15マスほど増やせる
3. 未使用の背景7枚（bowling / cabin_outside / country_station_dawn / cruise_lobby / ferris_cabin / forest_real / waterpark）は第2章（しゃかいじん・ろうご）用に温存
4. 頭＋体の2パーツ合成（枚数削減の本命）
5. 恋人→結婚ルート、しゃかいじん／ろうご の第2章
